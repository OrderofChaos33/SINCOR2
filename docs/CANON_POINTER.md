# Canon pointer

`TOKEN_CANON.json` is the only machine-readable money surface.
Generate a human digest with `python scripts/generate_canon_md.py`.

`FOCUS.md`, `HOLD.md`, `TOA_4TIER_CODEBASE_MEMORY.md`, and CEO briefs are strategy notes.
If they disagree with the JSON, the JSON wins. Do not copy holder counts into those files.
